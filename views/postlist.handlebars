<h2>All posts</h2>
<div class="container">
    <ul class="row g-2 row-cols-1 row-cols-md-2 row-cols-lg-3">
        {{#each posts as |post|}}
            {{> post-details}}
        {{/each}}
    </ul>
</div>